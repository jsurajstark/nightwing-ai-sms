# Logging (terminal: Ollama request/response lines). Use DEBUG for full model JSON.
LOG_LEVEL=INFO

# LLM
LLM_PROVIDER=ollama
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.1:8b-instruct-q4_K_M

# Future providers (uncomment when wired)
# ANTHROPIC_API_KEY=
# AWS_REGION=
# BEDROCK_MODEL_ID=

# Database
DATABASE_URL=sqlite:///./data/demo.db

# Twilio webhook (optional). When ENABLE_TWILIO_WEBHOOK=true, set token and public HTTPS base URL
# used to reconstruct the webhook URL for signature validation (e.g. ngrok URL, no trailing slash).
ENABLE_TWILIO_WEBHOOK=false
TWILIO_AUTH_TOKEN=
PUBLIC_BASE_URL=
